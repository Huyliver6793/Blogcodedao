<%= form_for [@micropost, @micropost.comments.build] do |f| %>
            <%= f.hidden_field :parent_id %>
                <p>
                    <%= f.label :body %><br>
                    <%= f.text_area :body %>
                </p>
                <p>
                    <%= f.submit "comment" %>
                </p>
        
<% end %>