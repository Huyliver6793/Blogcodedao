<div class="navbar-wrapper">
    <div class="container-fluid">
        <nav class="navbar navbar-fixed-top">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="#">Blog Codedao</a>
                </div>
                <div id="navbar" class="navbar-collapse collapse">
                    <% if logged_in? %>
                        <ul class="nav navbar-nav pull-right">
                            <li class=""><%= link_to @current_user.name, current_user %></li>
                            <li><%=link_to "Home", root_url %></li>
                            <li><%=link_to "Post Entry", post_path %></li>
                            <li><%=link_to "Settting", edit_user_path(current_user) %></li>
                            <li><%=link_to "Log out", logout_path,method: "delete" %></li>

                        </ul>
                    <% else %>
                    <ul class="nav navbar-nav pull-right">
                        <li><%=link_to "Home", root_url %></li>
                        <li><%= link_to "Register",signup_path,role: "button" %> </li>         
                        <li class=""><%= link_to "Login", login_path %></li>
                    </ul>
                    <% end %>
                </div>
            </div>
        </nav>
    </div>
</div>
